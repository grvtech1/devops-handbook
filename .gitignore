# This repository tracks the DevOps Handbook deliverable.
# Unrelated projects / nested repos that happen to live in the same folder are ignored,
# so a future `git add -A` never sweeps in secrets or someone else's project.

/billfree-techops/
/GAS webapp/
/microshop/
/kubernetes-projects-learning/
/.agents/
/.claude/
/.vscode/

# Leftover files from the old git-playground history (not part of the handbook)
/Code.gs
/Code1.gs
/Index.html
/Index.pre_spa_build_20260301_154700.html
/code1
/config.txt
/data.txt
/notes.txt
/cloudflare-pages/

# Old binary exports — the handbook markdown is the source of truth
*.docx
*.pdf

# OS / editor noise
.DS_Store
Thumbs.db
node_modules/
*.log

# Unreferenced diagram exports at repo root (used copies live in docs/assets/)
/*.svg
/devops_two_loops_overview.png
/devops_two_loops_overview2.png

# MkDocs build output — auto-generated by `mkdocs build`, never commit
/site/
